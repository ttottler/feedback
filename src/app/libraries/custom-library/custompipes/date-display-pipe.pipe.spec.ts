import { DateDisplayPipePipe } from './date-display-pipe.pipe';

describe('DateDisplayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateDisplayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
